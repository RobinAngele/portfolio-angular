export type skill = {
    skill: string,
    alt: string,
    iconUrl: string
}

export type project = {
    title: string,
    stack: string,
    description: string,
    github: string,
    projectUrl: string;
    prevImageUrl: string
}